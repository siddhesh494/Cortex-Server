       User
        │
    API Gateway                    → FastAPI (routes + CORS + JWT) — lightweight gateway
        │   
    Conversation Manager           → ChatService
        │
┌─────────┼──────────┐
Session   Memory     User      → ChatRepository / SummaryAgent+window / Auth user only
Store     Manager    Profile
  │
Prompt Builder                 → Inline in each agent (no shared builder service yet)
  │
Tool Decision Layer            → DecisionAgent + ToolRegistry
  │
┌──────┴──────┐
LLM Only   Tool Calling        → ResponseAgent alone  vs  SearchTool → then ResponseAgent
  │
Response Validator             → ❌ Not implemented yet
  │
MongoDB                        → users + chatSession collections